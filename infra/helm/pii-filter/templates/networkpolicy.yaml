{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Chart.Name }}
  policyTypes: [Ingress, Egress]
  ingress:
    # Only Kong may reach the agent.
    - from:
        - podSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.ingressFromLabel | nindent 14 }}
      ports:
        {{- range .Values.service.ports }}
        - port: {{ .targetPort }}
        {{- end }}
  egress:
    # Cluster DNS.
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # In-namespace services: pii-filter, mock-core-banking, Redis.
    - to:
        - podSelector: {}
    # OpenAI / external HTTPS.
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 443
{{- end }}
